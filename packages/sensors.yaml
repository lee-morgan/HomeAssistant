  ### Weather prediction ###
  - platform: yr

  ### Weather forecasts ###
  - platform: darksky 
    api_key: !secret darksky_api_key
    monitored_conditions:
      - summary
      - temperature
      - wind_speed
      - precip_probability
    scan_interval:
      hours: 1
    units: uk2
    
  ### Plex sensor - Who's watching and what! ###
  - platform: plex
    name: PlexSpy
    host: !secret plex_host_ip
    port: !secret plex_host_port
    username: !secret plex_username
    password: !secret plex_password
    token: 'cFq3vKTWxbGNds76BcFs'
    server: 'Home Media'

  ### Format data how i want it ###
  # - platform: template
  #   sensors:
  #     solar_angle:
  #       icon_template: mdi:angle_acute
  #       friendly_name: 'Sun angle'
  #       unit_of_measurement: '°'
  #       value_template: "{{ state_attr('sun.sun', 'elevation') }}"
  #     sunrise:
  #       icon_template: mdi:weather_sunset_up
  #       friendly_name: 'Sunrise'
  #       value_template: "{{as_timestamp(states.sun.sun.attributes.next_rising) | timestamp_custom('%H:%M')}}"
  #     sunset:
  #       icon_template: mdi:weather_sunset_down
  #       friendly_name: 'Sunset'
  #       value_template: "{{as_timestamp(states.sun.sun.attributes.next_setting) | timestamp_custom('%H:%M')}}"
      # uptime:
      #   icon_template: mdi:clock
      #   friendly_name: 'Uptime'
      #   value_template: "{{(as_timestamp(now())-as_timestamp(states.sensor.last_boot.state)) | timestamp_custom('%Hh %Mm')}}"


        